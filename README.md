# drpkgSkeleton

This is a package skeleton generated from `funcs_dr.R`.

## Suggested next steps

1. Rename the package in `DESCRIPTION`.
2. Run `devtools::document()` to generate `man/` and `NAMESPACE`.
3. Add examples and tests.
4. Review functions that rely on objects from the parent environment, such as `lfdr_thre()`.
