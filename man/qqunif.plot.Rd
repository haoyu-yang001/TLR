% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{qqunif.plot}
\alias{qqunif.plot}
\title{QQ plot against the uniform null distribution}
\usage{
qqunif.plot(
  pvalues,
  should.thin = T,
  thin.obs.places = 2,
  thin.exp.places = 2,
  xlab = expression(paste("Expected (", -log[10], ~p[beta[j]], ")")),
  ylab = expression(paste("Observed (", -log[10], ~p[beta[j]], ")")),
  draw.conf = TRUE,
  conf.points = 1000,
  conf.col = "lightgray",
  conf.alpha = 0.05,
  already.transformed = FALSE,
  pch = 20,
  aspect = "iso",
  prepanel = prepanel.qqunif,
  par.settings = list(superpose.symbol = list(pch = pch)),
  ...
)
}
\arguments{
\item{pvalues}{A numeric vector of p-values, or a named list of numeric
vectors of p-values. If a list is supplied, each element is plotted as
a separate group.}

\item{should.thin}{Logical; if \code{TRUE}, nearby points are thinned by
rounding the observed and expected coordinates before plotting. This
is useful for large-scale testing problems with many p-values.}

\item{thin.obs.places}{Integer; number of decimal places used when
rounding observed \eqn{-\log_{10}(p)} values for thinning.}

\item{thin.exp.places}{Integer; number of decimal places used when
rounding expected \eqn{-\log_{10}(p)} values for thinning.}

\item{xlab}{Label for the x-axis. By default this is the expected
\eqn{-\log_{10}(p)} under the uniform null distribution.}

\item{ylab}{Label for the y-axis. By default this is the observed
\eqn{-\log_{10}(p)}.}

\item{draw.conf}{Logical; if \code{TRUE}, adds a pointwise confidence
envelope under the null uniform distribution.}

\item{conf.points}{Integer; number of points used to draw the
confidence envelope.}

\item{conf.col}{Color used to fill the confidence envelope.}

\item{conf.alpha}{Significance level used for the confidence envelope.}

\item{already.transformed}{Logical; if \code{FALSE}, the input is
interpreted as raw p-values and transformed internally to
\eqn{-\log_{10}(p)}. If \code{TRUE}, the input is assumed to already
be on the \eqn{-\log_{10}(p)} scale.}

\item{pch}{Plotting character passed to \code{\link[lattice:xyplot]{lattice::xyplot()}}.}

\item{aspect}{Aspect ratio passed to \code{\link[lattice:xyplot]{lattice::xyplot()}}.}

\item{prepanel}{A prepanel function passed to \code{\link[lattice:xyplot]{lattice::xyplot()}}. The
default sets matched x- and y-axis ranges.}

\item{par.settings}{A list of graphical parameters passed to
\code{\link[lattice:xyplot]{lattice::xyplot()}}.}

\item{...}{Additional arguments passed to \code{\link[lattice:xyplot]{lattice::xyplot()}}.}
}
\value{
A \code{"trellis"} object produced by \code{\link[lattice:xyplot]{lattice::xyplot()}}.
}
\description{
Draws a quantile-quantile (QQ) plot comparing observed p-values with
their expected values under the null uniform distribution. The plot can
be used to assess calibration of p-values, visualize departure from the
null, and examine enrichment of small p-values in the tail.
}
\details{
For a single vector of p-values, the function plots the observed
ordered \eqn{-\log_{10}(p)} values against their expected order
statistics under the null uniform distribution. If a list of p-value
vectors is provided, each list element is displayed as a separate
group, allowing visual comparison across methods or simulation
settings.

In large-scale mediation testing and epigenome-wide studies, this plot
is useful for checking whether p-values are well calibrated under the
null and for visualizing excess small p-values that may indicate true
signals or model misspecification.
}
\examples{
set.seed(1)
p0 <- stats::runif(1000)
qqunif.plot(p0)

p.list <- list(
  Method1 = stats::runif(1000),
  Method2 = c(stats::runif(950), stats::runif(50, 0, 1e-3))
)
qqunif.plot(p.list)

}
